# 綜合資安威脅暨異常程序深度分析報告 (Comprehensive Threat & Suspicious Process Deep-Dive) 🌐🛡️🔬

**報告編號 (Report ID):** 2.2 (Merged)  
**日期 (Date):** 2026-05-04  
**分析範圍 (Scope):** 全量 8 類日誌關聯分析與 `endpoint_logs.csv` 深度探勘 / Full correlation across 8 log types and deep-dive into endpoint behavior.  
**威脅評級 (Threat level):** 極高 (Critical)

---

## 1. 執行摘要 (Executive Summary)

本報告整合了全域資安威脅概況與針對「異常程序執行」的深度鑑識結果。分析確認組織正遭受一場**多階段進階持續性威脅 (APT)** 活動。攻擊者以釣魚郵件與暴力破解為起點，成功進入內網後，大規模利用**程序偽裝 (Masquerading)** 與 **Living off the Land (LotL)** 策略建立持久化通道，並已進行顯著的敏感資料存取與外流。

This report integrates the global security threat landscape with deep-dive forensic findings on "Suspicious Process Execution." Analysis confirms a **multi-stage Advanced Persistent Threat (APT)** campaign. Starting with phishing and brute force, attackers established persistence using **Masquerading** and **LotL** strategies, leading to significant sensitive data access and exfiltration.

---

## 2. 全域攻擊鏈關聯分析 (Global Attack Chain Correlation)

根據跨日誌（Auth, DNS, Email, Endpoint, File, FW, Netflow, USB）的分析，攻擊鏈如下：

1.  **初始入侵 (Initial Access)**:
    *   **釣魚**: 偵測到 1,007 封 `urgent` 誘餌郵件。
    *   **暴力破解**: `10.0.0.11` 等 IP 對 `nancy` 等帳號進行密集嘗試（最高單一帳號 44 次失敗）。
2.  **執行與持續性 (Execution & Persistence)**: 
    *   利用編碼 PowerShell 與 `svchost.exe` 偽裝。
    *   **98 組 DNS 心跳信號** 指向外部 C2。
3.  **目標達成 (Actions on Objectives)**:
    *   針對 `.db` 與 `.sql` 的大量存取，`nancy` 風險評分達 50,660 分。
    *   **資料外流**: 單一主機外傳流量最高達 **590 MB**。

---

## 3. 異常程序執行深度鑽取 (Suspicious Process Deep-Dive)

本章節著重於攻擊者在端點系統的具體執行戰術：

### A. 偽裝調用模式統計 (Masquerading Patterns)
| 調用模式 (Event, Detail) | 出現次數 (Count) | 威脅技術 (Technique) |
| :--- | :--- | :--- |
| **cmd, svchost.exe** | 624 | 程序偽裝 (T1036.003) - 由普通使用者啟動系統服務程序。 |
| **cmd, backup.bat** | 616 | 批次腳本執行 - 疑似持久化或自動化清理。 |
| **powershell, svchost.exe** | 613 | 利用 PS 環境啟動服務程序進行隱匿。 |
| **powershell -enc ...** | 577 | 指令編碼 (T1027) - 規避基於關鍵字的監控。 |

### B. 高風險帳號與主機分佈
*   **重點調查帳號**: `kate` (438次)、`eve` (410次)、`nancy` (406次)、`leo` (404次)。
*   **重災區主機**: `WS02` (628次)、`WS01` (608次)。

### C. 技術觀察：LotL 策略應用
攻擊者避開了自定義惡意文件，轉而利用 `Excel` 或 `Chrome` 作為父程序來啟動 `cmd` 或 `powershell`。這種技術能有效繞過傳統僅依賴文件 Hash 的 Antivirus 系統。

---

## 4. 關鍵威脅指標 (Key Threat Indicators)

| 維度 (Vector) | 核心偵測結果 (Core Findings) | 嚴重程度 |
| :--- | :--- | :--- |
| **Identity** | 帳號 `nancy`、`charlie`、`grace` 確認失陷。 | Critical |
| **Endpoint** | 數千筆偽裝程序執行，集中於 Workstations 區。 | Critical |
| **Network** | 大規模資料外流 (>500MB) 與 98 組 C2 心跳。 | Critical |
| **Data** | 敏感資料庫 (.db, .sql) 遭到非授權存取。 | High |

---

## 5. 終端建議行動方案 (Final Recommendations)

### A. 即時處置與隔離 (Containment)
1.  **阻斷 C2 與隔離主機**: 阻斷異常 DNS 網域，並將外流量最高的主機（`10.0.0.10` 等）隔離。
2.  **帳號處理**: 立即停用 `nancy`、`kate`、`eve` 等高風險帳號。
3.  **進程清理**: 終止所有由非系統權限啟動的 `svchost.exe`。

### B. 偵測強化與監控 (Hardening)
1.  **程序親緣性規則 (Parent-Process Rules)**: 設置 EDR 警報，禁止 `cmd/powershell` 啟動系統關鍵進程。
2.  **PowerShell 審計**: 開啟 Script Block Logging 以解碼 `-enc` 指令。
3.  **身分驗證**: 強制重設全域憑證並部署 MFA。

---
*報告產出人 (Report Generated By): Gemini CLI*
